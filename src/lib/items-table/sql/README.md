# Legacy SQL copies

Canonical migrations live in [`supabase/migrations/`](../../../../supabase/migrations/).

Use the Supabase CLI (`npm run db:push`) rather than pasting these files. Kept only as a readable mirror of the first schema + TTL job; prefer the `supabase/` tree when editing.
