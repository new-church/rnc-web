# Public Outlook published calendar (.ics) — safe to expose; used for subscribe + site calendar
PUBLIC_CALENDAR_ICS_URL=
CALENDAR_ICS_URL=

# Supabase Data API (Project Settings → API). App runtime — not a Postgres URI.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Optional: browser / member login later
# PUBLIC_SUPABASE_URL=
# PUBLIC_SUPABASE_ANON_KEY=
